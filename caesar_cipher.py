def encode(text: str, shift: int) -> str:
    """Shift each letter forward by `shift` places. Build this up one test at a time."""
    raise NotImplementedError


def decode(text: str, shift: int) -> str:
    """Undo encode(text, shift)."""
    raise NotImplementedError
